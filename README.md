# football-quize
game đá bóng câu hỏi
git init
git add .
git commit -m "Initial commit - Football Quiz"
git branch -M main
git remote add origin https://github.com/<tên-người-dùng>/<tên-repo>.git
git push -u origin main
